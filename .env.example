# Copy to .env and fill in once the backend exists.
# Left unset, the app talks to its built-in mock API layer (src/api) instead —
# see reference/api/README.md for the contracts the mocks are standing in for.
EXPO_PUBLIC_API_BASE_URL=
